start = input("Choose working mode (Server or User): ")

if start == "Server":
    print()
else:
    if start == "User":
        print()
    else:
        start = input("Wrong choose. Choose again (type \"Server\" or \"User\"): ")
